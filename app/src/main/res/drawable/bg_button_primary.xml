<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="@color/click_blue"/> <!-- Pressed color -->
            <stroke android:color="@color/click_blue" android:width="1dp"/>
            <corners android:radius="10dp"/>
        </shape>
    </item>

    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/click_blue"/> <!-- Pressed color -->
            <stroke android:color="@color/click_blue" android:width="1dp"/>
            <corners android:radius="10dp"/>
        </shape>
    </item>
    <!-- Focused state -->
    <item android:state_focused="true">
        <shape>
            <solid android:color="@color/click_blue"/> <!-- Focused color -->
            <stroke android:color="@color/click_blue" android:width="1dp"/>
            <corners android:radius="10dp"/>
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="@color/blue"/> <!-- Default color -->
            <corners android:radius="10dp"/>
        </shape>
    </item>
</selector>