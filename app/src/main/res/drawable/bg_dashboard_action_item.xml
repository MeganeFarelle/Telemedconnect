<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
<!-- Pressed state -->
<item android:state_pressed="true">
    <shape>
        <solid android:color="@color/light_grey"/> <!-- Pressed color -->
        <corners android:radius="8dp"/>
    </shape>
</item>
<!-- Focused state -->
<item android:state_focused="true">
    <shape>
        <solid android:color="@color/light_grey"/> <!-- Focused color -->
        <corners android:radius="8dp"/>
    </shape>
</item>
<!-- Default state -->
<item>
    <shape>
        <solid android:color="#00FFFFFF"/> <!-- Default color -->
    </shape>
</item>
</selector>